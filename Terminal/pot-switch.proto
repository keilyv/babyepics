terminator = CR LF;

info{
   ExtraInput = Ignore;
   out "ID?";
   in "%39c";
}

getValue {
   out "POT?";
   in "%d";
}

getState{
   out "ROC?";
   in "%d";
}

debug{
   ExtraInput = Ignore;
   out "%s";
   in "%39c";
}
